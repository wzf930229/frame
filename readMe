文件介绍：
1、data_*.xls表格用来存放访问url,data等参数，可编辑，但格式固定，且表格名称固定为"data_*",不可改变；
2、data_*.xls表格中的data列与requestData_*.json文件的key值一一对应；
3、public_url.json文件用于存放公共的url。
方法调用：
1、post(row,f1,f2)适用于不需要传入token且url中不带参数的post请求，如用户登陆；
2、post_t(row,f1,f2)适用于需要传入token且url中不带参数的post请求；
3、get(row,f1,f2)适用于不需要传入token且url中不带参数的get请求；
4、get_v(row,id,f1,f2)适用于需要传入token且url中带参数的get请求；
5、post_v(row,id,f1,f2)适用于需要传入token且url中带参数的get请求；
6、post_c(row,f1,f2,l,li)适用于参数体中某个键的值参数化的情况；
7、get_t(row,f1,f2)适用于需要传入token且url中不带参数的get请求；
8、write2excel(row,content，f2)进行断言，并将执行结果写入到文件中。
注：其中row为data_*.xls中的行数，id为url中所带的参数,f1代指requestData_*.json文件，f2代指data_*.xls表格，l、li分别是两个数组，l用于存放键，li用于存放值。